# Microsoft Exchange Online MCP Server Configuration
# Copy this file to .env and fill in your values

# Required: Azure AD App Registration Client ID
# Create an app registration at: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
EXCHANGE_CLIENT_ID=your-client-id-here

# Optional: Azure AD Tenant ID
# Use your organization's tenant ID, or "common" for multi-tenant (default: common)
EXCHANGE_TENANT_ID=your-tenant-id-or-common

# Optional: Token cache location (default: ~/.mcp-exchange/token_cache.json)
# EXCHANGE_TOKEN_CACHE=/path/to/token_cache.json

# Optional: HTTP request timeout in seconds (default: 30)
# EXCHANGE_TIMEOUT=30

# Optional: Timezone for date/time display (IANA format, default: America/Chicago)
# All dates and times in tool outputs will be converted to this timezone
EXCHANGE_TIMEZONE=America/Chicago
